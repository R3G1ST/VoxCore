<UserControl
    x:Class="VoxCore.Client.VoiceGraphView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="Root" Background="#0a0a12" SizeChanged="Root_SizeChanged">
        <Grid.Resources>
            <LinearGradientBrush x:Key="NeonStroke" StartPoint="0,0" EndPoint="1,0">
                <GradientStop Color="#00ff88" Offset="0" />
                <GradientStop Color="#00e5ff" Offset="0.5" />
                <GradientStop Color="#00ff88" Offset="1" />
            </LinearGradientBrush>
            <LinearGradientBrush x:Key="NeonText" StartPoint="0,0" EndPoint="1,0">
                <GradientStop Color="#00ff88" Offset="0" />
                <GradientStop Color="#00e5ff" Offset="0.5" />
                <GradientStop Color="#00ff88" Offset="1" />
            </LinearGradientBrush>
            <Style x:Key="NeonBtn" TargetType="Button">
                <Setter Property="Background" Value="#0a0a1290" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="14,8" />
                <Setter Property="FontFamily" Value="Cascadia Code" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Foreground" Value="#e0e0f0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Canvas x:Name="StarsCanvas" />
        <Canvas x:Name="EdgesCanvas" />
        <Canvas x:Name="PulsesCanvas" />
        <Canvas x:Name="NodesCanvas" />

        <!-- HEADER -->
        <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,14,0,0">
            <Button x:Name="HubHomeBtn" Style="{StaticResource NeonBtn}" BorderBrush="{StaticResource NeonStroke}"
                    Width="44" Height="36" Padding="0" Click="HubHomeBtn_Click" ToolTipService.ToolTip="главная — секторы, передачи, союзники">
                <Border Background="#5865f2" CornerRadius="6" Width="22" Height="22">
                    <TextBlock Text="V" FontFamily="Cascadia Code" FontWeight="Bold" Foreground="White"
                               FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
            </Button>
            <Border Padding="24,9" CornerRadius="12" BorderThickness="1"
                    BorderBrush="{StaticResource NeonStroke}" Background="#0a0a1290">
                <TextBlock Text="ECHO NEXUS | AUDIO HUB" FontFamily="Cascadia Code" FontSize="15"
                           FontWeight="Bold" Foreground="{StaticResource NeonText}" />
            </Border>
            <Button x:Name="HubSettingsBtn" Style="{StaticResource NeonBtn}" BorderBrush="{StaticResource NeonStroke}"
                    Width="44" Height="36" Padding="0" Click="GearBtn_Click" ToolTipService.ToolTip="навигационная консоль">
                <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="19" />
            </Button>
        </StackPanel>

        <!-- ACTIVE STREAM -->
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="18,0,0,18" Spacing="6" Width="280">
            <TextBlock Text="ACTIVE STREAM:" FontFamily="Cascadia Code" FontSize="11" Foreground="#00ff88" />
            <StackPanel x:Name="StreamList" Spacing="6" />
            <Border x:Name="NoStreamBorder" Background="#0a0a1260" BorderBrush="#1a1a30" BorderThickness="1"
                    CornerRadius="8" Padding="10,6" HorizontalAlignment="Left">
                <TextBlock Text="тишина — никто не передаёт" FontFamily="Cascadia Code" FontSize="11" Foreground="#4a4a6a" />
            </Border>
        </StackPanel>

        <!-- CENTER CONTROLS -->
        <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,18">
            <Button x:Name="MicBtn" Style="{StaticResource NeonBtn}" BorderBrush="{StaticResource NeonStroke}" Click="MicBtn_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="&#xE720;" FontFamily="Segoe MDL2 Assets" FontSize="15" VerticalAlignment="Center" />
                    <TextBlock x:Name="MicText" Text="MICROPHONE ACTIVE" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button x:Name="SpkBtn" Style="{StaticResource NeonBtn}" BorderBrush="#00e5ff" Click="SpkBtn_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="&#xE767;" FontFamily="Segoe MDL2 Assets" FontSize="15" VerticalAlignment="Center" />
                    <TextBlock x:Name="SpkText" Text="SPEAKER" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- USERS + END CALL -->
        <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,18,18">
            <Border Background="#0a0a1290" BorderBrush="#1a1a30" BorderThickness="1" CornerRadius="10" Padding="12,8">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="#4a4a6a" VerticalAlignment="Center" />
                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="15" Foreground="#00ff88" VerticalAlignment="Center" />
                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="#4a4a6a" VerticalAlignment="Center" />
                    <TextBlock x:Name="UsersCountText" Text="0" FontFamily="Cascadia Code" FontSize="12"
                               Foreground="#e0e0f0" VerticalAlignment="Center" Margin="4,0,0,0" />
                </StackPanel>
            </Border>
            <Button x:Name="EndCallBtn" Style="{StaticResource NeonBtn}" BorderBrush="#ff3b5c" Foreground="#ff5c78"
                    Click="EndCallBtn_Click" ToolTipService.ToolTip="покинуть сектор">
                <TextBlock Text="END CALL" FontWeight="Bold" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
