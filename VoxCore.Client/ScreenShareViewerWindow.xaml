<Window
    x:Class="VoxCore.Client.ScreenShareViewerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#1e1f22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#232428" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border Width="10" Height="10" CornerRadius="5" Background="#3ba55d" VerticalAlignment="Center"
                        Margin="0,0,8,0" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                    <TextBlock Text="Демонстрация" FontFamily="Cascadia Code" FontSize="13" FontWeight="SemiBold"
                               Foreground="#dbdee1" />
                    <TextBlock x:Name="SharerNameText" FontFamily="Cascadia Code" FontSize="13" Foreground="#949ba4"
                               VerticalAlignment="Center" />
                </StackPanel>
                <Button Grid.Column="2" Content="✕" Width="28" Height="28" Padding="0" Background="Transparent"
                        BorderThickness="0" Foreground="#949ba4" Click="CloseBtn_Click" />
            </Grid>
        </Border>

        <!-- Screen -->
        <Grid Grid.Row="1" Background="#1e1f22">
            <Image x:Name="ScreenImage" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBlock x:Name="StatusText" Text="ожидание потока..." FontFamily="Cascadia Code" FontSize="14"
                       Foreground="#949ba4" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Window>
