# Тестирование сборки VoxCore (portable)

## Запуск
1. Скачай артефакт `VoxCore-client-win-x64-...` (вкладка Actions → нужный commit → Artifacts)
2. Распакуй zip в любую папку
3. Запусти `VoxCore.Client.exe`

## Включить DeepFilterNet3 (нейро-шумодав)
DLL не лежит рядом с exe специально (ломает старт приложения). Включи так:
1. Нажми `Win+R` → вставь `%LOCALAPPDATA%\VoxCore\native` → Enter (создай папку, если нет)
2. Скопируй туда `redist\deep_filter_ladspa.dll` из архива
3. Перезапусти VoxCore → Настройки → «DeepFilterNet3: загружен» (зелёный)

Без этого шага всё работает, кроме нейро-шумодава (RNNoise остаётся активным).

## Что проверить
- [ ] Вход/регистрация
- [ ] Клик на голосовой канал → статус «WebRTC подключен» (не UDP)
- [ ] Настройки → панель «ГОЛОСОВОЙ PIPELINE»: все пункты зелёные
- [ ] Голос без хрипов и прерываний (тест с собеседником)
- [ ] Тест микрофона (шкала уровня), «слушать себя»
- [ ] Чат канала, ЛС, друзья

## Логи для багрепортов
- Клиент WebRTC: `%TEMP%\voxcore-client.log`
- Краши: `%TEMP%\voxcore-client-crash.log`
